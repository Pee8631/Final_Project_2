// Code generated by entc, DO NOT EDIT.

package ent

import (
	"FinalProject/ent/treatment"
	"FinalProject/ent/user"
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TreatmentCreate is the builder for creating a Treatment entity.
type TreatmentCreate struct {
	config
	mutation *TreatmentMutation
	hooks    []Hook
}

// SetTreatmentRecord sets the "treatmentRecord" field.
func (tc *TreatmentCreate) SetTreatmentRecord(s string) *TreatmentCreate {
	tc.mutation.SetTreatmentRecord(s)
	return tc
}

// SetDateTime sets the "dateTime" field.
func (tc *TreatmentCreate) SetDateTime(t time.Time) *TreatmentCreate {
	tc.mutation.SetDateTime(t)
	return tc
}

// SetNillableDateTime sets the "dateTime" field if the given value is not nil.
func (tc *TreatmentCreate) SetNillableDateTime(t *time.Time) *TreatmentCreate {
	if t != nil {
		tc.SetDateTime(*t)
	}
	return tc
}

// SetTakeTime sets the "takeTime" field.
func (tc *TreatmentCreate) SetTakeTime(f float64) *TreatmentCreate {
	tc.mutation.SetTakeTime(f)
	return tc
}

// SetTreatmentWasRecordedByDoctorID sets the "treatment_was_recorded_by_doctor" edge to the User entity by ID.
func (tc *TreatmentCreate) SetTreatmentWasRecordedByDoctorID(id int) *TreatmentCreate {
	tc.mutation.SetTreatmentWasRecordedByDoctorID(id)
	return tc
}

// SetNillableTreatmentWasRecordedByDoctorID sets the "treatment_was_recorded_by_doctor" edge to the User entity by ID if the given value is not nil.
func (tc *TreatmentCreate) SetNillableTreatmentWasRecordedByDoctorID(id *int) *TreatmentCreate {
	if id != nil {
		tc = tc.SetTreatmentWasRecordedByDoctorID(*id)
	}
	return tc
}

// SetTreatmentWasRecordedByDoctor sets the "treatment_was_recorded_by_doctor" edge to the User entity.
func (tc *TreatmentCreate) SetTreatmentWasRecordedByDoctor(u *User) *TreatmentCreate {
	return tc.SetTreatmentWasRecordedByDoctorID(u.ID)
}

// SetUserIsTheTreatmentOfRecordID sets the "user_is_the_treatment_of_record" edge to the User entity by ID.
func (tc *TreatmentCreate) SetUserIsTheTreatmentOfRecordID(id int) *TreatmentCreate {
	tc.mutation.SetUserIsTheTreatmentOfRecordID(id)
	return tc
}

// SetNillableUserIsTheTreatmentOfRecordID sets the "user_is_the_treatment_of_record" edge to the User entity by ID if the given value is not nil.
func (tc *TreatmentCreate) SetNillableUserIsTheTreatmentOfRecordID(id *int) *TreatmentCreate {
	if id != nil {
		tc = tc.SetUserIsTheTreatmentOfRecordID(*id)
	}
	return tc
}

// SetUserIsTheTreatmentOfRecord sets the "user_is_the_treatment_of_record" edge to the User entity.
func (tc *TreatmentCreate) SetUserIsTheTreatmentOfRecord(u *User) *TreatmentCreate {
	return tc.SetUserIsTheTreatmentOfRecordID(u.ID)
}

// Mutation returns the TreatmentMutation object of the builder.
func (tc *TreatmentCreate) Mutation() *TreatmentMutation {
	return tc.mutation
}

// Save creates the Treatment in the database.
func (tc *TreatmentCreate) Save(ctx context.Context) (*Treatment, error) {
	var (
		err  error
		node *Treatment
	)
	if len(tc.hooks) == 0 {
		if err = tc.check(); err != nil {
			return nil, err
		}
		node, err = tc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*TreatmentMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = tc.check(); err != nil {
				return nil, err
			}
			tc.mutation = mutation
			if node, err = tc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(tc.hooks) - 1; i >= 0; i-- {
			if tc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = tc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, tc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (tc *TreatmentCreate) SaveX(ctx context.Context) *Treatment {
	v, err := tc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tc *TreatmentCreate) Exec(ctx context.Context) error {
	_, err := tc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tc *TreatmentCreate) ExecX(ctx context.Context) {
	if err := tc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tc *TreatmentCreate) check() error {
	if _, ok := tc.mutation.TreatmentRecord(); !ok {
		return &ValidationError{Name: "treatmentRecord", err: errors.New(`ent: missing required field "Treatment.treatmentRecord"`)}
	}
	if _, ok := tc.mutation.TakeTime(); !ok {
		return &ValidationError{Name: "takeTime", err: errors.New(`ent: missing required field "Treatment.takeTime"`)}
	}
	return nil
}

func (tc *TreatmentCreate) sqlSave(ctx context.Context) (*Treatment, error) {
	_node, _spec := tc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (tc *TreatmentCreate) createSpec() (*Treatment, *sqlgraph.CreateSpec) {
	var (
		_node = &Treatment{config: tc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: treatment.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: treatment.FieldID,
			},
		}
	)
	if value, ok := tc.mutation.TreatmentRecord(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: treatment.FieldTreatmentRecord,
		})
		_node.TreatmentRecord = value
	}
	if value, ok := tc.mutation.DateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: treatment.FieldDateTime,
		})
		_node.DateTime = value
	}
	if value, ok := tc.mutation.TakeTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: treatment.FieldTakeTime,
		})
		_node.TakeTime = value
	}
	if nodes := tc.mutation.TreatmentWasRecordedByDoctorIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   treatment.TreatmentWasRecordedByDoctorTable,
			Columns: []string{treatment.TreatmentWasRecordedByDoctorColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: user.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.user_doctor_record_treatment = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := tc.mutation.UserIsTheTreatmentOfRecordIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   treatment.UserIsTheTreatmentOfRecordTable,
			Columns: []string{treatment.UserIsTheTreatmentOfRecordColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: user.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.user_user_have_treatment = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// TreatmentCreateBulk is the builder for creating many Treatment entities in bulk.
type TreatmentCreateBulk struct {
	config
	builders []*TreatmentCreate
}

// Save creates the Treatment entities in the database.
func (tcb *TreatmentCreateBulk) Save(ctx context.Context) ([]*Treatment, error) {
	specs := make([]*sqlgraph.CreateSpec, len(tcb.builders))
	nodes := make([]*Treatment, len(tcb.builders))
	mutators := make([]Mutator, len(tcb.builders))
	for i := range tcb.builders {
		func(i int, root context.Context) {
			builder := tcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TreatmentMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tcb *TreatmentCreateBulk) SaveX(ctx context.Context) []*Treatment {
	v, err := tcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tcb *TreatmentCreateBulk) Exec(ctx context.Context) error {
	_, err := tcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tcb *TreatmentCreateBulk) ExecX(ctx context.Context) {
	if err := tcb.Exec(ctx); err != nil {
		panic(err)
	}
}
