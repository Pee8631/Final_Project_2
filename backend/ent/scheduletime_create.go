// Code generated by entc, DO NOT EDIT.

package ent

import (
	"FinalProject/ent/schedule"
	"FinalProject/ent/scheduletime"
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ScheduleTimeCreate is the builder for creating a ScheduleTime entity.
type ScheduleTimeCreate struct {
	config
	mutation *ScheduleTimeMutation
	hooks    []Hook
}

// SetStartTime sets the "startTime" field.
func (stc *ScheduleTimeCreate) SetStartTime(t time.Time) *ScheduleTimeCreate {
	stc.mutation.SetStartTime(t)
	return stc
}

// SetStopTime sets the "stopTime" field.
func (stc *ScheduleTimeCreate) SetStopTime(t time.Time) *ScheduleTimeCreate {
	stc.mutation.SetStopTime(t)
	return stc
}

// SetWhatTimeIsTheScheduleID sets the "what_time_is_the_schedule" edge to the Schedule entity by ID.
func (stc *ScheduleTimeCreate) SetWhatTimeIsTheScheduleID(id int) *ScheduleTimeCreate {
	stc.mutation.SetWhatTimeIsTheScheduleID(id)
	return stc
}

// SetNillableWhatTimeIsTheScheduleID sets the "what_time_is_the_schedule" edge to the Schedule entity by ID if the given value is not nil.
func (stc *ScheduleTimeCreate) SetNillableWhatTimeIsTheScheduleID(id *int) *ScheduleTimeCreate {
	if id != nil {
		stc = stc.SetWhatTimeIsTheScheduleID(*id)
	}
	return stc
}

// SetWhatTimeIsTheSchedule sets the "what_time_is_the_schedule" edge to the Schedule entity.
func (stc *ScheduleTimeCreate) SetWhatTimeIsTheSchedule(s *Schedule) *ScheduleTimeCreate {
	return stc.SetWhatTimeIsTheScheduleID(s.ID)
}

// Mutation returns the ScheduleTimeMutation object of the builder.
func (stc *ScheduleTimeCreate) Mutation() *ScheduleTimeMutation {
	return stc.mutation
}

// Save creates the ScheduleTime in the database.
func (stc *ScheduleTimeCreate) Save(ctx context.Context) (*ScheduleTime, error) {
	var (
		err  error
		node *ScheduleTime
	)
	if len(stc.hooks) == 0 {
		if err = stc.check(); err != nil {
			return nil, err
		}
		node, err = stc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ScheduleTimeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = stc.check(); err != nil {
				return nil, err
			}
			stc.mutation = mutation
			if node, err = stc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(stc.hooks) - 1; i >= 0; i-- {
			if stc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = stc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, stc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (stc *ScheduleTimeCreate) SaveX(ctx context.Context) *ScheduleTime {
	v, err := stc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (stc *ScheduleTimeCreate) Exec(ctx context.Context) error {
	_, err := stc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (stc *ScheduleTimeCreate) ExecX(ctx context.Context) {
	if err := stc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (stc *ScheduleTimeCreate) check() error {
	if _, ok := stc.mutation.StartTime(); !ok {
		return &ValidationError{Name: "startTime", err: errors.New(`ent: missing required field "startTime"`)}
	}
	if _, ok := stc.mutation.StopTime(); !ok {
		return &ValidationError{Name: "stopTime", err: errors.New(`ent: missing required field "stopTime"`)}
	}
	return nil
}

func (stc *ScheduleTimeCreate) sqlSave(ctx context.Context) (*ScheduleTime, error) {
	_node, _spec := stc.createSpec()
	if err := sqlgraph.CreateNode(ctx, stc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (stc *ScheduleTimeCreate) createSpec() (*ScheduleTime, *sqlgraph.CreateSpec) {
	var (
		_node = &ScheduleTime{config: stc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: scheduletime.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: scheduletime.FieldID,
			},
		}
	)
	if value, ok := stc.mutation.StartTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: scheduletime.FieldStartTime,
		})
		_node.StartTime = value
	}
	if value, ok := stc.mutation.StopTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: scheduletime.FieldStopTime,
		})
		_node.StopTime = value
	}
	if nodes := stc.mutation.WhatTimeIsTheScheduleIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   scheduletime.WhatTimeIsTheScheduleTable,
			Columns: []string{scheduletime.WhatTimeIsTheScheduleColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: schedule.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.schedule_time_schedule = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ScheduleTimeCreateBulk is the builder for creating many ScheduleTime entities in bulk.
type ScheduleTimeCreateBulk struct {
	config
	builders []*ScheduleTimeCreate
}

// Save creates the ScheduleTime entities in the database.
func (stcb *ScheduleTimeCreateBulk) Save(ctx context.Context) ([]*ScheduleTime, error) {
	specs := make([]*sqlgraph.CreateSpec, len(stcb.builders))
	nodes := make([]*ScheduleTime, len(stcb.builders))
	mutators := make([]Mutator, len(stcb.builders))
	for i := range stcb.builders {
		func(i int, root context.Context) {
			builder := stcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ScheduleTimeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, stcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, stcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, stcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (stcb *ScheduleTimeCreateBulk) SaveX(ctx context.Context) []*ScheduleTime {
	v, err := stcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (stcb *ScheduleTimeCreateBulk) Exec(ctx context.Context) error {
	_, err := stcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (stcb *ScheduleTimeCreateBulk) ExecX(ctx context.Context) {
	if err := stcb.Exec(ctx); err != nil {
		panic(err)
	}
}
